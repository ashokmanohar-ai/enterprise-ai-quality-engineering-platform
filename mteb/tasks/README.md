# Embedding evaluation scope

The external benchmark shortlist uses small retrieval tasks (`NFCorpus`, `SciFact`) by default to control time and cost. The decisive gate is the AcmeCloud application retrieval benchmark plus Ragas context recall/precision. MTEB results never substitute for application evidence.
