# PyRIT executors

PyRIT 1.0 refactored orchestration components toward executors. This directory intentionally uses that current terminology even though some industry material still says orchestrator. `PromptSendingAttack` is the PR smoke example; adaptive and multi-turn executors belong in authorized nightly/manual profiles.
